// the config file is located on /etc/v2ray/config.json or
//  /usr/local/etc/v2ray/config/json
{
    //  "log" : {
        //    "access": "/var/log/v2ray/access.log",
        //    "error": "/var/log/v2ray/error.log",
        //    "loglevel": "warn"
        //  },
        "inbound": {
            "port": xxxx,    // listen port
            "protocol": "vmess",
            "settings": {
                "clients": [
                    {
                        "id": "xxxx-uuid-xxxx",   // can be get by `cat /proc/sys/kernel/random/uuid`
                        // or from `uuidgenerator.net`
                        "level": 1,
                        "alterId": 64
                    }
                ]
            }
        },
        "outbound": {
            "protocol": "freedom",
            "settings": {}
        },
        "outboundDetour": [
            {
                "protocol": "blackhole",
                "settings": {},
                "tag": "blocked"
            }
        ]
}

